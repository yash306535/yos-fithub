<!-- trainerdashboard.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trainer Profile</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            color: #333;
        }

        h2 {
            color: #007BFF;
        }

        .profile-image {
            text-align: center;
            margin-bottom: 20px;
        }

        .profile-image img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #007BFF;
        }

        .info-section {
            margin-top: 20px;
        }

        .info-section input {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
            resize: vertical;
        }

        .info-section input[readonly] {
            background-color: #f4f4f4;
        }

        /* Add more styling as needed */
    </style>
</head>
<body>
    

    <% if (trainerProfile) { %>
        <div class="container">
            <h1>Trainer Profile</h1>
            <h2><%= trainerProfile.trainerName %></h2>
    
            <div class="info-section">
                <label for="specialization">Specialization:</label>
                <input type="text" id="specialization" value="<%= trainerProfile.trainerSpecialization %>" readonly>
    
                <label for="experience">Experience:</label>
                <input type="text" id="experience" value="<%= trainerProfile.trainerExperience %> years" readonly>
    
                <label for="certification">Certification:</label>
                <input type="text" id="certification" value="<%= trainerProfile.trainerCertification %>" readonly>
    
                <label for="email">Email:</label>
                <input type="email" id="email" value="<%= trainerProfile.trainerEmail %>" readonly>
    
                <label for="phone">Phone:</label>
                <input type="tel" id="phone" value="<%= trainerProfile.trainerPhone %>" readonly>
    
                <label for="location">Location:</label>
                <input type="text" id="location" value="<%= trainerProfile.trainerLocation %>" readonly>
    
                <label for="availability">Availability:</label>
                <input type="text" id="availability" value="<%= trainerProfile.trainerAvailability %>" readonly>
    
                <label for="rate">Rate:</label>
                <input type="text" id="rate" value="<%= trainerProfile.trainerRate %>" readonly>
    
                <label for="description">Description:</label>
                <textarea id="description" readonly><%= trainerProfile.trainerDescription %></textarea>
            </div>
        </div>
    <% } else { %>
        <p>No trainer profile found.</p>
    <% } %>
    
      

</body>
<script type="module">
    // Import the functions you need from the SDKs you need
    import { initializeApp } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-app.js";
    import { getAnalytics } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-analytics.js";
    // TODO: Add SDKs for Firebase products that you want to use
    // https://firebase.google.com/docs/web/setup#available-libraries
  
    // Your web app's Firebase configuration
    // For Firebase JS SDK v7.20.0 and later, measurementId is optional
    const firebaseConfig = {
      apiKey: "AIzaSyByo-y7JgD3KAHxe08y22SdoLMKVKrkYbI",
      authDomain: "yos-fithub.firebaseapp.com",
      projectId: "yos-fithub",
      storageBucket: "yos-fithub.appspot.com",
      messagingSenderId: "164748123569",
      appId: "1:164748123569:web:1d537542679e841fb53190",
      measurementId: "G-6XHVH3DC08"
    };
  
    // Initialize Firebase
    const app = initializeApp(firebaseConfig);
    const analytics = getAnalytics(app);
  </script>
</html>
