<!-- trainerdashboard.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trainer Profile</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            color: #333;
        }

        h2 {
            color: #007BFF;
        }

        .profile-image {
            text-align: center;
            margin-bottom: 20px;
        }

        .profile-image img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #007BFF;
        }

        .info-section {
            margin-top: 20px;
        }

        .info-section input {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
            resize: vertical;
        }

        .info-section input[readonly] {
            background-color: #f4f4f4;
        }

        /* Add more styling as needed */
    </style>
</head>
<body>
    
    <% if (trainerProfile) { %>
        <div class="container">
            <h1>Trainer Profile</h1>
            <h2><%= trainerProfile.trainerName %></h2>
    
            <div class="info-section">
                <p><strong>Specialization:</strong> <%= trainerProfile.trainerSpecialization %></p>
                <p><strong>Experience:</strong> <%= trainerProfile.trainerExperience %> years</p>
                <p><strong>Certification:</strong> <%= trainerProfile.trainerCertification %></p>
                <p><strong>Email:</strong> <%= trainerProfile.trainerEmail %></p>
                <p><strong>Phone:</strong> <%= trainerProfile.trainerPhone %></p>
                <p><strong>Location:</strong> <%= trainerProfile.trainerLocation %></p>
                <p><strong>Availability:</strong> <%= trainerProfile.trainerAvailability %></p>
                <p><strong>Rate:</strong> <%= trainerProfile.trainerRate %></p>
                <div>
                    <strong>Description:</strong>
                    <p><%= trainerProfile.trainerDescription %></p>
                </div>
            </div>
        </div>
    <% } else { %>
        <p>No trainer profile found.</p>
    <% } %>
    
    
      

</body>
<script type="module">
    // Import the functions you need from the SDKs you need
    import { initializeApp } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-app.js";
    import { getAnalytics } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-analytics.js";
    // TODO: Add SDKs for Firebase products that you want to use
    // https://firebase.google.com/docs/web/setup#available-libraries
  
    // Your web app's Firebase configuration
    // For Firebase JS SDK v7.20.0 and later, measurementId is optional
    const firebaseConfig = {
      apiKey: "AIzaSyByo-y7JgD3KAHxe08y22SdoLMKVKrkYbI",
      authDomain: "yos-fithub.firebaseapp.com",
      projectId: "yos-fithub",
      storageBucket: "yos-fithub.appspot.com",
      messagingSenderId: "164748123569",
      appId: "1:164748123569:web:1d537542679e841fb53190",
      measurementId: "G-6XHVH3DC08"
    };
  
    // Initialize Firebase
    const app = initializeApp(firebaseConfig);
    const analytics = getAnalytics(app);
  </script>
</html>
